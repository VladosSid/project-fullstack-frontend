import Footer from './Footer';

export const App = props => {
  return (
    <div
      style={{
        height: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        fontSize: 40,
        color: '#010101',
      }}
    >
      <Footer />
      React homework template
    </div>
  );
};
